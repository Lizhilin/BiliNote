# 加载优化记录

## 前端

### 1. JS 分包

**文件：** `BillNote_frontend/vite.config.ts`

**问题：** 首页加载 3.1 MB JS（未压缩），其中 `react-markdown` + 语法高亮 + KaTeX 等 1MB+ 在首页就被加载。

**优化：** 将 `MarkdownViewer`（包含 `react-markdown` 等重型依赖）从静态 import 改为 `React.lazy`，只在用户点击笔记时才加载。

```tsx
// 改前
import MarkdownViewer from '@/pages/HomePage/components/MarkdownViewer.tsx'

// 改后
const MarkdownViewer = lazy(() => import('@/pages/HomePage/components/MarkdownViewer.tsx'))
```

**效果：** 首页加载 JS 从 3.1 MB → 1.1 MB，gzip 后从 892 KB → 331 KB。

### 2. ChatPanel 懒加载（独立 chunk）

**文件：** `BillNote_frontend/src/pages/HomePage/components/MarkdownViewer.tsx`

**问题：** ChatPanel 引入 `@ant-design/x`（含 antd 依赖 ~400-600KB），被 MarkdownViewer 静态 import 导致打包进同一个 chunk。

**优化：** ChatPanel 改为 `React.lazy`，`@ant-design/x` 独立成 `ChatPanel-COslJas1.js` chunk。

```tsx
// 改前
import ChatPanel from '@/pages/HomePage/components/ChatPanel.tsx'

// 改后
const ChatPanel = lazy(() => import('@/pages/HomePage/components/ChatPanel.tsx'))
```

**效果：**

| Chunk | 改前 | 改后 |
|------|------|------|
| MarkdownViewer | 765KB (160KB gzip) | 451KB (58KB gzip) |
| ChatPanel（新） | — | 315KB (102KB gzip)，按需加载 |

### 3. Nginx 压缩 + 缓存

**文件：** `BillNote_frontend/deploy/default.conf`

**优化：**
- 开启 gzip 压缩 JS/CSS/JSON/SVG
- 带 hash 的静态资源（`/assets/*`）设置 `Cache-Control: public, immutable`，缓存 1 月

```nginx
gzip on;
gzip_types text/css application/javascript application/json image/svg+xml;

location /assets/ {
  expires 1M;
  add_header Cache-Control "public, immutable";
}
```

### 4. CDN 缓存预热

**文件：** `构建.js` — `warmupCache()`

**问题：** 每次部署后新 chunk 文件名 hash 变化，Cloudflare 无缓存，首次访问需回源内网（ZeroTier 延迟高）。

**优化：** 部署后自动通过服务器向 Cloudflare 请求所有 `.js`/`.css` 资源，触发 Cloudflare 回源缓存。

```js
const cmds = urls.map(url => `curl -s 'https://domain/assets/${url}'`);
ssh(cmds.join(' && ')).catch(() => {});
```

### 5. 图片直连 + 缓存兜底

**文件：** `BillNote_frontend/.env` → `VITE_IMAGE_PROXY_ENABLED`

**优先级逻辑：**
- 关闭代理（默认）：直连 B 站 CDN → `referrerPolicy="no-referrer"` 绕过防盗链 → 加载失败时 onerror 回退服务端缓存
- 开启代理：服务端缓存（`static/cover_cache/`）→ 回源 B 站

### 6. KaTeX CSS 动态加载

**文件：** `BillNote_frontend/src/pages/HomePage/components/MarkdownViewer.tsx`

**优化：** KaTeX CSS 从静态 import 改为 `useEffect` 动态 import，只在有笔记内容时加载。

```tsx
useEffect(() => {
  if (selectedContent) {
    import('katex/dist/katex.min.css')
  }
}, [selectedContent])
```

### 7. 历史列表分离 markdown + transcript

**文件：** `backend/app/routers/note.py`、`BillNote_frontend/src/store/taskStore/index.ts`

**优化：** 历史列表 `/task_history` 不再返回 markdown（~6KB/条），点击详情时才通过 `/task_detail` 懒加载。
详情 `/task_detail` 默认排除 transcript（~50KB），只有打开转写面板时才按需加载 `/task_detail/{id}/transcript`。

## 后端

### 1. 图片代理缓存

**文件：** `backend/app/routers/system.py` — `/api/image_proxy`

**优化：** 代理请求图片时保存到 `static/cover_cache/`，后续请求直接返回本地文件。

```python
cache_path, ext = _cache_path(url)
if ext and os.path.exists(cache_path):
    return FileResponse(cache_path, ...)
```

### 2. 删除任务清理缓存

**文件：** `backend/app/routers/note.py` — `/delete_task`

**优化：** 删除任务时同时清理 `static/cover_cache/` 中对应的封面缓存。

### 3. 列表仅读取元数据（计划中）

当前 `/task_history` 仍对每个文件执行完整 `json.load()`（含 transcript），后续可用独立元数据索引优化。

## 测量指标

| 指标 | 优化前 | 优化后 |
|------|--------|--------|
| 首页 JS 加载量 | 3.1 MB / 892 KB gzip | 1.1 MB / 331 KB gzip |
| MarkdownViewer chunk | 765 KB / 160 KB gzip | 451 KB / 58 KB gzip |
| ChatPanel chunk | 打包在 MarkdownViewer 中 | 315 KB / 102 KB gzip，按需加载 |
| 详情页 API 响应 | ~65 KB（含 transcript） | ~6 KB（不含 transcript） |
| 首页请求数 | ~70（含 KaTeX 字体） | ~40 |
| 封面图加载 | 穿 ZeroTier 回源 B 站 | 直连 B 站 CDN |
| CDN 缓存 | 未利用 | 部署即预热 |
