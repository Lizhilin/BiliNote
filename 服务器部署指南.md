# BiliNote 服务器部署指南

## 服务器信息

| 项目 | 值 |
|------|-----|
| 服务器地址 | `10.3.104.112` |
| SSH 用户 | `root` |
| 项目目录 | `/root/docker-run-config/BiliNote/` |
| 对外访问端口 | `3015`（通过 nginx 反向代理） |

## 架构概览

三容器编排，由 `docker-compose.yml` 定义：

| 容器名 | 服务 | 说明 |
|--------|------|------|
| `bilinote-backend` | backend | FastAPI 后端，端口 8483 |
| `bilinote-frontend` | frontend | 前端静态资源，端口 80 |
| `bilinote-nginx` | nginx | 反向代理，对外 3015 → 80 |

数据持久化通过宿主机卷挂载：

```yaml
volumes:
  - ./data:/app/data               # 数据库
  - ./data/static:/app/static      # 截图
  - ./data/uploads:/app/uploads    # 上传文件
  - ./data/models:/app/models      # Whisper 模型缓存
  - ./data/note_results:/app/note_results  # 笔记导出
```

---

## 方式一：完整构建（推荐用于正式发布）

适用于：依赖变更、首次部署、正式发布版本。

### 1. 同步代码到服务器

```bash
# 在本地项目根目录执行
scp -r backend/ root@10.3.104.112:/root/docker-run-config/BiliNote/backend/
```

或只同步修改的文件：

```bash
scp backend/app/downloaders/bilibili_downloader.py root@10.3.104.112:/root/docker-run-config/BiliNote/backend/app/downloaders/
scp backend/app/services/note.py root@10.3.104.112:/root/docker-run-config/BiliNote/backend/app/services/
```

### 2. 构建并重启后端容器

```bash
ssh root@10.3.104.112

cd /root/docker-run-config/BiliNote/

# --no-cache 确保完全重建（约 2 分钟，主要是 pip install）
docker-compose build --no-cache backend && docker-compose up -d backend
```

### 3. 验证

```bash
# 检查容器状态（等待约 30 秒后应为 healthy）
docker ps --filter name=bilinote-backend --format "table {{.Names}}\t{{.Status}}"

# 健康检查
curl -f http://localhost:8483/api/sys_health

# 查看启动日志
docker logs --tail 30 bilinote-backend
```

---

## 方式二：快速更新（推荐用于热修复验证）

适用于：仅修改 Python 源码、无新增依赖、快速验证修复效果。

> 原理：用 `docker cp` 直接替换运行中容器内的文件，然后重启容器使改动生效。
> 耗时约 5 秒（对比完整构建的 2 分钟）。

### 1. 同步代码到服务器宿主机

```bash
scp backend/app/downloaders/bilibili_downloader.py root@10.3.104.112:/root/docker-run-config/BiliNote/backend/app/downloaders/
scp backend/app/services/note.py root@10.3.104.112:/root/docker-run-config/BiliNote/backend/app/services/
```

### 2. 将文件复制进容器并重启

```bash
ssh root@10.3.104.112

# 复制修改的文件到容器内
docker cp /root/docker-run-config/BiliNote/backend/app/downloaders/bilibili_downloader.py bilinote-backend:/app/app/downloaders/bilibili_downloader.py
docker cp /root/docker-run-config/BiliNote/backend/app/services/note.py bilinote-backend:/app/app/services/note.py

# 重启容器让代码生效
docker restart bilinote-backend
```

### 3. 验证

```bash
# 等待容器启动（约 10 秒）
sleep 10

# 检查状态
docker ps --filter name=bilinote-backend --format "table {{.Names}}\t{{.Status}}"

# 验证代码已更新（示例：检查超时配置）
docker exec bilinote-backend grep -n 'socket_timeout' /app/app/downloaders/bilibili_downloader.py
```

### 注意事项

- `docker cp` 的改动**仅在当前容器生效**，容器重建后会丢失
- 因此**必须同时更新宿主机源码**（第 1 步），确保下次完整构建时包含修改
- 如果修改涉及新增 Python 依赖，必须使用方式一完整构建

---

## 一键部署脚本

可以将常用操作封装为脚本，在本地项目根目录执行：

### 完整构建部署

```bash
# sync_and_rebuild.sh
scp -r backend/ root@10.3.104.112:/root/docker-run-config/BiliNote/backend/
ssh root@10.3.104.112 "cd /root/docker-run-config/BiliNote && docker-compose build --no-cache backend && docker-compose up -d backend"
```

### 快速更新部署

```bash
# quick_update.sh — 替换 FILE1 FILE2 为实际修改的文件路径
FILES="backend/app/downloaders/bilibili_downloader.py backend/app/services/note.py"
REMOTE_DIR="/root/docker-run-config/BiliNote"

for f in $FILES; do
  scp "$f" "root@10.3.104.112:$REMOTE_DIR/$f"
  ssh root@10.3.104.112 "docker cp $REMOTE_DIR/$f bilinote-backend:/app/${f#backend/}"
done

ssh root@10.3.104.112 "docker restart bilinote-backend"
```

---

## 常用运维命令

```bash
# 查看所有 BiliNote 容器状态
docker ps --filter name=bilinote --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"

# 查看后端日志（实时跟踪）
docker logs -f --tail 50 bilinote-backend

# 重启单个服务
docker restart bilinote-backend

# 重启所有 BiliNote 服务
cd /root/docker-run-config/BiliNote && docker-compose restart

# 停止所有服务
cd /root/docker-run-config/BiliNote && docker-compose down

# 启动所有服务
cd /root/docker-run-config/BiliNote && docker-compose up -d

# 进入后端容器调试
docker exec -it bilinote-backend bash

# 清理旧镜像
docker image prune -f
```

---

## 故障排查

| 问题 | 排查命令 |
|------|----------|
| 容器启动失败 | `docker logs bilinote-backend` |
| 健康检查不通过 | `curl -f http://localhost:8483/api/sys_health` |
| 前端无法访问后端 | 检查 nginx 配置：`docker exec bilinote-nginx cat /etc/nginx/conf.d/default.conf` |
| 数据库丢失 | 确认 `./data/` 卷挂载正常：`ls -la /root/docker-run-config/BiliNote/data/` |
| 模型加载失败 | 确认模型文件：`ls -la /root/docker-run-config/BiliNote/data/models/` |
