# BiliNote 服务器部署指南

## 服务器信息

| 项目 | 值 |
|------|-----|
| 服务器地址 | `10.3.104.112` |
| SSH 用户 | `root` |
| 项目目录 | `/docker-run-config/BiliNote/` |
| 对外访问端口 | `3015`（通过 nginx 反向代理） |

## 架构概览

三容器编排，由 `docker-compose.yml` 定义：

| 容器名 | 服务 | 说明 |
|--------|------|------|
| `bilinote-backend` | backend | FastAPI 后端，端口 8483 |
| `bilinote-frontend` | frontend | 前端静态资源，端口 80 |
| `bilinote-nginx` | nginx | 反向代理，对外 3015 → 80 |

数据持久化通过宿主机卷挂载：

```yaml
volumes:
  - ./data:/app/data               # 数据库
  - ./data/static:/app/static      # 截图
  - ./data/uploads:/app/uploads    # 上传文件
  - ./data/models:/app/models      # Whisper 模型缓存
  - ./data/note_results:/app/note_results  # 笔记导出
```

---

## 镜像分层策略

依赖层已拆分为独立 base 镜像（`bilinote-backend-base` / `bilinote-frontend-base`），日常代码构建无需下载依赖。

```bash
# 仅首次或修改 requirements.txt/pnpm-lock.yaml 时：
docker build -f backend/Dockerfile.base -t bilinote-backend-base .
docker build -f BillNote_frontend/Dockerfile.base -t bilinote-frontend-base .

# 日常代码构建（秒级，无网络）：
docker-compose build backend frontend
docker-compose up -d
```

## 按变更类型部署

> `docker-compose build` 已无网络下载（依赖在 base 镜像），日常代码变更只需 build + up。

### ① 前端代码变更

```bash
# 本地同步源码到服务器
cd BillNote_frontend && tar czf /tmp/frontend-src.tar.gz src/ package.json pnpm-lock.yaml vite.config.ts tsconfig*.json index.html
scp /tmp/frontend-src.tar.gz root@10.3.104.112:/tmp/
ssh root@10.3.104.112 "
  cd /docker-run-config/BiliNote
  tar xzf /tmp/frontend-src.tar.gz -C BillNote_frontend/
  rm /tmp/frontend-src.tar.gz
  docker-compose build frontend
  docker-compose up -d frontend
"
rm /tmp/frontend-src.tar.gz
```

### ② 后端 Python 代码变更

```bash
# 同步后端源码
scp -r backend/app/ root@10.3.104.112:/docker-run-config/BiliNote/backend/

# 服务器上 build 并重启
ssh root@10.3.104.112 "
  cd /docker-run-config/BiliNote
  docker-compose build backend
  docker-compose up -d backend
"
```

### ③ 依赖变更（需重建 base 镜像）

当 `requirements.txt`、`pnpm-lock.yaml` 或 `package.json` 变更时：

```bash
# 同步代码
scp -r backend/ root@10.3.104.112:/docker-run-config/BiliNote/backend/

# 服务器上重建 base + app
ssh root@10.3.104.112 "
  cd /docker-run-config/BiliNote
  docker build -f backend/Dockerfile.base -t bilinote-backend-base .
  docker-compose build backend
  docker-compose up -d backend
"
```

前端同理：

```bash
scp BillNote_frontend/package.json BillNote_frontend/pnpm-lock.yaml root@10.3.104.112:/docker-run-config/BiliNote/BillNote_frontend/
ssh root@10.3.104.112 "
  cd /docker-run-config/BiliNote
  docker build -f BillNote_frontend/Dockerfile.base -t bilinote-frontend-base .
  docker-compose build frontend
  docker-compose up -d frontend
"
```

---

## 常用运维命令

```bash
# 查看所有 BiliNote 容器状态
docker ps --filter name=bilinote --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"

# 查看后端日志（实时跟踪）
docker logs -f --tail 50 bilinote-backend

# 重启单个服务
docker restart bilinote-backend

# 重启所有 BiliNote 服务
cd /docker-run-config/BiliNote && docker-compose restart

# 停止所有服务
cd /docker-run-config/BiliNote && docker-compose down

# 启动所有服务
cd /docker-run-config/BiliNote && docker-compose up -d

# 进入后端容器调试
docker exec -it bilinote-backend bash

# 清理旧镜像
docker image prune -f
```

---

## 故障排查

| 问题 | 排查命令 |
|------|----------|
| 容器启动失败 | `docker logs bilinote-backend` |
| 健康检查不通过 | `curl -f http://localhost:8483/api/sys_health` |
| 前端无法访问后端 | 检查 nginx 配置：`docker exec bilinote-nginx cat /etc/nginx/conf.d/default.conf` |
| 数据库丢失 | 确认 `./data/` 卷挂载正常：`ls -la /docker-run-config/BiliNote/data/` |
| 模型加载失败 | 确认模型文件：`ls -la /docker-run-config/BiliNote/data/models/` |
| build 后代码没生效 | 确认 `docker-compose build` 没有因缓存跳过——改过的文件确认在 `COPY` 范围内 |
