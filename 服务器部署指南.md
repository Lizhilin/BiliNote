# BiliNote 服务器部署指南

## 服务器信息

| 项目 | 值 |
|------|-----|
| 服务器地址 | `10.3.104.112` |
| SSH 用户 | `root` |
| 项目目录 | `/root/docker-run-config/BiliNote/` |
| 对外访问端口 | `3015`（通过 nginx 反向代理） |

## 架构概览

三容器编排，由 `docker-compose.yml` 定义：

| 容器名 | 服务 | 说明 |
|--------|------|------|
| `bilinote-backend` | backend | FastAPI 后端，端口 8483 |
| `bilinote-frontend` | frontend | 前端静态资源，端口 80 |
| `bilinote-nginx` | nginx | 反向代理，对外 3015 → 80 |

数据持久化通过宿主机卷挂载：

```yaml
volumes:
  - ./data:/app/data               # 数据库
  - ./data/static:/app/static      # 截图
  - ./data/uploads:/app/uploads    # 上传文件
  - ./data/models:/app/models      # Whisper 模型缓存
  - ./data/note_results:/app/note_results  # 笔记导出
```

---

## 最佳实践：按变更类型选择策略

> **核心原则：代码改动用 `docker cp`，依赖改动才 rebuild。**
> 容器是无状态的（数据挂载在 volume），rebuild 浪费时间且无副作用。

### ① 前端代码变更（最频繁）

前端是纯静态文件，**不需要 rebuild 容器**，只需替换 dist。

```bash
# 本地：构建前端
cd BillNote_frontend && npx vite build

# 本地：打包并发送到服务器
tar czf /tmp/frontend-dist.tar.gz -C dist .
scp /tmp/frontend-dist.tar.gz root@10.3.104.112:/tmp/

# 服务器：解压并复制到容器
ssh root@10.3.104.112 "
  mkdir -p /tmp/bilinote-dist
  tar xzf /tmp/frontend-dist.tar.gz -C /tmp/bilinote-dist/
  docker cp /tmp/bilinote-dist/. bilinote-frontend:/usr/share/nginx/html/
  rm -rf /tmp/bilinote-dist /tmp/frontend-dist.tar.gz
  docker exec bilinote-frontend nginx -s reload
"

# 本地：清理
rm /tmp/frontend-dist.tar.gz
```

耗时约 30 秒（主要是 vite build）。

### ② 后端 Python 代码变更

后端不改依赖时，`docker cp` 直接替换文件 plus 重启即可。

```bash
# 以修改了 backend/app/routers/note.py 为例：

# 同步到宿主机（保留副本，下次 rebuild 才有）
scp backend/app/routers/note.py root@10.3.104.112:/root/docker-run-config/BiliNote/backend/app/routers/

# 复制进容器并重启
ssh root@10.3.104.112 "
  docker cp /root/docker-run-config/BiliNote/backend/app/routers/note.py bilinote-backend:/app/app/routers/note.py
  docker restart bilinote-backend
"
```

耗时约 5 秒。

如果需要同步多个文件：

```bash
# 本地打包后端目录发过去，服务器解压后逐个 docker cp
tar czf /tmp/backend-code.tar.gz backend/app/
scp /tmp/backend-code.tar.gz root@10.3.104.112:/root/docker-run-config/BiliNote/
ssh root@10.3.104.112 "
  cd /root/docker-run-config/BiliNote
  tar xzf backend-code.tar.gz
  docker cp backend/app/. bilinote-backend:/app/app/
  docker restart bilinote-backend
  rm -f backend-code.tar.gz
"
```

### ③ 依赖变更（需要 rebuild）

需要 rebuild 的场景：

- `backend/requirements.txt` 或 `backend/Dockerfile` 变更
- `BillNote_frontend/package.json` 或 `Dockerfile` 变更
- `.env` 新增环境变量
- 部署新版本 / 正式发布

```bash
# 同步代码到服务器
scp -r backend/ root@10.3.104.112:/root/docker-run-config/BiliNote/backend/

# rebuild 并重启
ssh root@10.3.104.112 "
  cd /root/docker-run-config/BiliNote
  docker-compose build --no-cache backend
  docker-compose up -d backend
"
```

前端依赖变更同理：

```bash
# 前端需要全量同步（npm install 需要 package.json + lockfile）
cd BillNote_frontend && npx vite build
tar czf /tmp/frontend-dist.tar.gz -C dist .
scp /tmp/frontend-dist.tar.gz root@10.3.104.112:/tmp/
ssh root@10.3.104.112 "
  cd /root/docker-run-config/BiliNote
  docker-compose build --no-cache frontend
  docker-compose up -d frontend
"
```

### ④ 同时更新前后端

```bash
# 本地构建前端
cd BillNote_frontend && npx vite build

# 打包前端 dist + 后端代码
tar czf /tmp/frontend-dist.tar.gz -C dist .
tar czf /tmp/backend-code.tar.gz backend/
scp /tmp/frontend-dist.tar.gz root@10.3.104.112:/tmp/
scp /tmp/backend-code.tar.gz root@10.3.104.112:/root/docker-run-config/BiliNote/

# 服务器：更新前端 + 后端
ssh root@10.3.104.112 "
  # 前端：docker cp
  mkdir -p /tmp/bilinote-dist
  tar xzf /tmp/frontend-dist.tar.gz -C /tmp/bilinote-dist/
  docker cp /tmp/bilinote-dist/. bilinote-frontend:/usr/share/nginx/html/
  rm -rf /tmp/bilinote-dist /tmp/frontend-dist.tar.gz
  docker exec bilinote-frontend nginx -s reload

  # 后端：docker cp + restart
  cd /root/docker-run-config/BiliNote
  tar xzf backend-code.tar.gz
  docker cp backend/app/. bilinote-backend:/app/app/
  rm -f backend-code.tar.gz
  docker restart bilinote-backend
"
```

---

## 一键部署脚本

```bash
# save as bin/deploy.sh 并按需编辑
```

### 前端更新

```bash
#!/usr/bin/env bash
set -e
cd BillNote_frontend && npx vite build
tar czf /tmp/frontend-dist.tar.gz -C dist .
scp /tmp/frontend-dist.tar.gz root@10.3.104.112:/tmp/
ssh root@10.3.104.112 "
  mkdir -p /tmp/bilinote-dist
  tar xzf /tmp/frontend-dist.tar.gz -C /tmp/bilinote-dist/
  docker cp /tmp/bilinote-dist/. bilinote-frontend:/usr/share/nginx/html/
  rm -rf /tmp/bilinote-dist /tmp/frontend-dist.tar.gz
  docker exec bilinote-frontend nginx -s reload
"
rm /tmp/frontend-dist.tar.gz
```

### 后端代码更新

```bash
#!/usr/bin/env bash
set -e
tar czf /tmp/backend-code.tar.gz backend/app/
scp /tmp/backend-code.tar.gz root@10.3.104.112:/root/docker-run-config/BiliNote/
ssh root@10.3.104.112 "
  cd /root/docker-run-config/BiliNote
  tar xzf backend-code.tar.gz
  docker cp backend/app/. bilinote-backend:/app/app/
  rm -f backend-code.tar.gz
  docker restart bilinote-backend
"
rm /tmp/backend-code.tar.gz
```

---

## 常用运维命令

```bash
# 查看所有 BiliNote 容器状态
docker ps --filter name=bilinote --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"

# 查看后端日志（实时跟踪）
docker logs -f --tail 50 bilinote-backend

# 重启单个服务
docker restart bilinote-backend

# 重启所有 BiliNote 服务
cd /root/docker-run-config/BiliNote && docker-compose restart

# 停止所有服务
cd /root/docker-run-config/BiliNote && docker-compose down

# 启动所有服务
cd /root/docker-run-config/BiliNote && docker-compose up -d

# 进入后端容器调试
docker exec -it bilinote-backend bash

# 清理旧镜像
docker image prune -f
```

---

## 故障排查

| 问题 | 排查命令 |
|------|----------|
| 容器启动失败 | `docker logs bilinote-backend` |
| 健康检查不通过 | `curl -f http://localhost:8483/api/sys_health` |
| 前端无法访问后端 | 检查 nginx 配置：`docker exec bilinote-nginx cat /etc/nginx/conf.d/default.conf` |
| 数据库丢失 | 确认 `./data/` 卷挂载正常：`ls -la /root/docker-run-config/BiliNote/data/` |
| 模型加载失败 | 确认模型文件：`ls -la /root/docker-run-config/BiliNote/data/models/` |
| docker cp 后代码没生效 | 确认容器名正确并已 restart |
